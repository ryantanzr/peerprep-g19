# Firebase project ID (required for JWT token verification)
FIREBASE_PROJECT_ID=your-project-id

# Note: The collaboration service uses jose (not firebase-admin) for token
# verification. It only needs the project ID to validate the issuer/audience
# claims. No private key or service account is needed.
